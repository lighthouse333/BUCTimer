# 项目：学业功能（考试 / 成绩 / 绩点）实现方案

> 本文件是「学业」Tab 三个待实现功能（考试查询、成绩查询、绩点查询）的实现方案，基于现有「教务系统在线导入」链路设计，供后续开发会话直接续接。

---

## 1. 现状与可复用资产

在线导入课表的登录与会话链路已经打通并随 v2.0.0 发布（测试中），学业功能**不新建链路，全部复用**：

| 资产 | 位置 | 复用方式 |
|---|---|---|
| WebView 统一认证登录 | `ui/TimetableScreen.kt` `OnlineImportWebDialog`（私有，需抽出） | 抽成共用组件，学业页复用 |
| 会话 Cookie 持久化 | `jw/JwSessionStore.kt`（只存 Cookie 不存密码） | 直接复用，建议增存学号 |
| 教务 JSON API 客户端 | `jw/JwApiClient.kt`（HttpURLConnection + 浏览器 UA + Referer） | 新增 `fetchGrades` / `fetchExams` |
| 课表接口与学期码 | `kbcx/xskbcx_cxXsKb.html?gnmkdm=N2145`，xqm 3=秋冬/12=春夏/16=暑假 | 成绩、考试接口同套路，xnm/xqm/su 参数一致 |
| JSON 解析范式 | `importer/BuctJsonTimetableParser.kt`（org.json，字段容错） | 新解析器照此风格写 |
| 编排范式 | `TimetableViewModel.importOnlineTimetable`（IO 协程 → 解析 → State 流） | 新 `AcademicsViewModel` 照此风格写 |
| 学业页入口 | `ui/ClassScheduleApp.kt:109` `AcademicsComingSoonScreen` 占位（三功能条目已列出） | 替换为真实页面 |
| 参考项目 | `C:\Users\Lenovo\Desktop\code\classschedule-outport`（Node+Playwright，`src/auth.js` 登录链、`src/schedule.js` 接口调用） | 抓包/调试时对照 |
| 测试基线 | `ExampleUnitTest.kt`（xqm 映射、课表 JSON 解析已有用例） | 新增解析器与 GPA 用例 |

关键约束（沿用既有约定）：密码不落入 App（WebView 内登录）；会话只存 Cookie；数据仅本地。

---

## 2. 阶段 0：抓包确认接口（关键前置，决定后续一切）

教务系统为正方 jwglxt，**接口路径与功能号（gnmkdm）因学校部署而异，不能凭通用知识硬编码**。做法：

1. PC Chrome 登录 `jwglxt.buct.edu.cn` → F12 → Network，勾选 Preserve log。
2. 依次打开「我的成绩」（含等级成绩/全部成绩切换）和「考试安排」页面，记录：
   - 请求 URL 路径 + `gnmkdm` 值
   - 请求方法：GET query 还是 POST form（正方惯例为 POST，body 含 `xnm`/`xqm`；现有课表接口是 GET + query，新接口以抓包为准，两种都要能发）
   - 响应 JSON 完整样本（存入测试资源，做单测语料）
3. 候选接口（仅供对照，以抓包为准）：
   - 成绩：`/jwglxt/kccj/xscjcx_cxXsKccjList.html` 一类（gnmkdm 疑似 N30500x）
   - 考试：`/jwglxt/ksgl/xskscx_cxXsKs.html` 一类
4. **同时确认绩点口径**：若成绩响应自带每门课 `jd`（绩点）字段，绩点直接用接口值；否则需查北化教务处的百分位→绩点换算表。口径拿不准时，App 内绩点标注「仅供参考」，不得宣称与教务处一致。

---

## 3. 阶段 1：网络与数据层（纯 Kotlin，可先行 + 配单测）

### 3.1 `jw/JwApiClient.kt` 扩展
- 增加 POST form 支持（`xnm=..&xqm=..` 表单体），保留现有 GET 路径。
- 新增 `fetchGrades(cookies, studentId, year, xqm)`、`fetchExams(cookies, studentId, ...)`，URL 常量集中在文件顶部。
- **会话过期识别**：Cookie 失效时接口通常返回登录页 HTML 或跳转。在 `fetchText` 中统一检测（响应非 JSON / 含统一认证特征），抛类型化的 `JwSessionExpiredException`，供 UI 区分「重新登录」与普通错误。

### 3.2 新建 `jw/JwModels.kt`
- `BuctGrade`：课程名、课程类别、学分（xf）、成绩（cj）、绩点（jd，可空）、学年（xn）、学期（xqm）、性质/类别等。
- `BuctExam`：课程名、考试时间（kssj）、地点（cdmc）、座位号（zwh）、考试类型等。
- `GpaSummary`：加权平均绩点 = Σ(绩点×学分) / Σ学分，支持按学年学期聚合。

### 3.3 新建解析器（仿 `BuctJsonTimetableParser`）
- `jw/BuctGradeParser.kt`、`jw/BuctExamParser.kt`：JSON → 模型，字段容错。
- 注意 `cj` 可能是数字（89.5）也可能是等级制（优/良/中/及格/不及格/合格），两种都要能解析；等级制成绩无绩点时按学校换算表映射或置空。

### 3.4 单元测试
- 用抓包样本 JSON 做语料：等级制成绩、缺字段、空列表、会话过期响应样本。
- GPA 计算边界（0 学分、无绩点课程剔除）。
- 运行：`./gradlew :app:testDebugUnitTest --console=plain`（现有基线须保持全绿）。

---

## 4. 阶段 2：登录复用重构

1. 把 `TimetableScreen.kt` 里的私有 `OnlineImportWebDialog` 抽成共用 `ui/jw/JwLoginOverlay.kt`（行为不变），课表在线导入改用它并**回归验证**。
2. `JwSessionStore` 增存**学号**（`su` 参数每次请求都要用；学号非密码，可存）与可选的登录时间。
3. 学业页会话状态机：`未登录 →（WebView 登录）→ 已登录`；接口抛 `JwSessionExpiredException` 时回到「会话已过期，请重新登录」。
4. 教务系统会话有效期较短（通常几十分钟），过期重新走一遍 WebView 登录是常态，UI 要把这个流程做得轻。

---

## 5. 阶段 3：学业页 UI

- `ClassScheduleApp.kt:109` 的占位替换为 `AcademicsScreen(academicsViewModel)`。
- 新建 `ui/AcademicsViewModel.kt`（持 `JwSessionStore`，状态：登录态 / 加载 / 数据 / 错误）与 `ui/AcademicsScreen.kt`：
  - 顶部：登录/学生信息卡（姓名、学号、专业、班级——课表接口的 `xsxx` 已含这些字段，可顺带展示）。
  - Tab 或分段：「考试」「成绩」。
  - 成绩页：顶部绩点汇总卡（加权平均绩点 + 总学分），下方按学年学期分组的成绩列表，支持学期筛选与手动刷新。
  - 考试页：按时间排序的卡片（课程 / 时间 / 地点 / 座位号）。
  - 空态、加载态、错误态齐全；错误区分「会话过期」（按钮直接重新登录）与普通失败（重试）。

---

## 6. 阶段 4：验证与收尾

- `./gradlew :app:assembleDebug` → 真机（学校 VPN 开启）全链路验证：学业页 → 登录 / 免登复用 → 成绩 / 考试拉取 → 绩点 → 手动过期会话后重登。
- 对照教务系统网页端核对成绩条数、绩点数值口径。
- **隐私**：成绩、考试数据仅在内存中展示，不写数据库、不上传；README「学业功能规划」小节与 CHANGELOG 同步更新，规划版本号。

---

## 7. 里程碑顺序与风险

**顺序**：阶段 0 抓包 → 阶段 1 网络层 + 解析 + 单测 → 阶段 2 登录重构 → 阶段 3 UI → 阶段 4 打磨收尾。阶段 1 不依赖真机，可先行。

**风险与对策**：
1. 接口路径/gnmkdm 与通用正方系统不同 → 一切以抓包为准，URL 集中管理便于改。
2. 新接口要求 POST form 而现有客户端只会 GET → 阶段 1 先补 POST 能力。
3. 会话短时效 → 过期检测 + 一键重登，不做静默续期。
4. WebView 登录链路此前在真机上出现过打不开的问题（见《项目：接入北化教务系统自动导入的说明.md》第 5 节），虽然已随 v2.0.0 发布，学业功能开工前应先回归现有「在线导入」全链路。
5. 绩点口径与教务处不一致 → 优先用接口自带 jd，否则明确标注「仅供参考」。
6. 成绩属敏感数据 → 不落库、不上传，仅内存展示。

**建议分支**：从当前 `main` 切 `agent/buct-academics`。
