# 项目：接入北化教务系统自动导入的说明

> 本文件记录了「在 ClassSchedule Android App 中接入北京化工大学教务系统，实现在线拉取课表并自动导入」这一任务的目标、参考项目、已完成/未完成工作与遇到的问题，供下次 AI 会话直接续接。

---

## 1. 任务目标

在 App（`C:\Users\Lenovo\Desktop\ClassSchedule`，Kotlin + Jetpack Compose，包名 `com.example.timetable`）内实现「**在线导入**」：

- 用户在 App 内输入**学号、学年、学期**，随后在**内嵌 WebView 完成北化统一身份认证登录**（密码直接交给学校，App 不接触、不逆向 SM2）。
- 登录成功后拿到**会话 Cookie**，用纯 HTTP 调教务系统的 JSON API 拉取课表，解析成 `Course` 列表，走现有「预览 → 导入」流程入库。
- **会话层复用**：Cookie 持久化，为后续「成绩查询」等功能铺路（一次登录，多处复用）。
- 已确认：**只保留在线导入**，不做「导入 JSON 文件」通道。

---

## 2. 借鉴的（参考）项目

`C:\Users\Lenovo\Desktop\code\classschedule-outport` —— Node.js + Playwright 命令行工具，自动化登录北化教务系统并导出课表 JSON。

### 2.1 文件结构

```
src/auth.js          登录：portal.buct.edu.cn → iframe 登录 → i.buct.edu.cn → 点「教务管理系统」→ 新标签开 jwglxt
src/schedule.js      调教务 JSON API，映射字段、合并课程
src/index.js         CLI 入口（命令行/交互式）
scripts/query-config.js / examples/*.js   批量/示例
output/schedule_*.json   已生成的课表示例（含 2026-2027-1、2025-2026-1、2025-2026-3）
```

### 2.2 关键结论（已提取，直接可用）

**登录流程（auth.js）**：
```
https://portal.buct.edu.cn/  （iframe #iframeObj 里填学号/密码，密码由页面 JS 用 SM2 国密加密）
  → 跳 i.buct.edu.cn（i北化门户）
  → 点击「教务管理系统」
  → 新标签页打开 jwglxt.buct.edu.cn
```

**课表接口（schedule.js）**：
```js
const JW_BASE = 'https://jwglxt.buct.edu.cn';
// URL:
`${JW_BASE}/jwglxt/kbcx/xskbcx_cxXsKb.html?gnmkdm=N2145&layout=default&su=${学号}&xnm=${起始年}&xqm=${学期码}`
// 返回体：页面 body 即 JSON，含 kbList[]（课表数组）+ xsxx{}（学生信息）
```

**学期码映射（关键陷阱，不可直传 1/2/3）**：
```
用户语义学期 1=秋冬 → xqm="3"
            2=春夏 → xqm="12"
            3=暑假 → xqm="16"
```

**单门课 JSON 字段 → 映射关系（schedule.js mapRecord）**：
```
name        ← kcmc     名称
day         ← xqjmc    星期（"星期一"~"星期日"）
periods     ← jcs      节次（如 "3-5"）
weeks       ← zcd      周次（如 "1-9周,11-17周"，可含 "(单)"/"(双)"）
location    ← cdmc     场地（可能为 "未排地点"）
campus      ← xqmc     校区
teacher     ← xm       教师
credits     ← xf       学分
category    ← kclb     课程类别
nature      ← kcxz     课程性质
kind        ← xslxbj   类型标记（★讲课 ○实验 ●实践 ◇上机）
teachingClass ← jxbmc  教学班
```

**学生信息字段**：`xsxx.XM(姓名) XH(学号) ZYMC(专业) BJMC(班级) NJDM_ID(年级) JSXM(辅导员)`。

---

## 3. 已完成工作

全部改动**尚未 commit**（工作区未提交、未 push）。

### 3.1 App 端已落地代码

| 文件 | 状态 | 说明 |
|---|---|---|
| `importer/BuctJsonTimetableParser.kt` | 改 | 重构为 `object`，纯「字符串 JSON → `ParsedTimetable`」；成员 `parseBuctJson(text,totalWeeks)`、`parseWeekText`（支持 `(单)/(双)`）、`dayToWeekDay`、`parsePeriods` |
| `importer/TimetableImportSchool.kt` | 改 | 已撤销临时加的「北京化工大学（JSON）」文件导入枚举项，只剩 PDF/XLSX 三家 |
| `jw/JwApiClient.kt` | 新 | `object`：`xqmForSemester(Int)`、`scheduleUrl(...)`、`suspend fetchSchedule(cookies,学号,年,xqm)`（HttpURLConnection + Cookie/Referer/浏览器UA + 15s/30s 超时） |
| `jw/JwSessionStore.kt` | 新 | `SharedPreferences("jw_session")` 存取会话 Cookie：`saveCookies/loadCookies/clear` |
| `ui/TimetableViewModel.kt` | 改 | 新增 `importOnlineTimetable(cookies,学号,年,学期)`（拉取→`parseBuctJson`→`Success(parsed)`→存 Cookie）；`hasSavedJwSession()/savedJwSession()`；撤销了文件导入 map 项 |
| `ui/TimetableScreen.kt` | 改 | 新增「在线导入」菜单项 → 学号/学年/学期弹窗 `OnlineImportInfoDialog` → WebView 登录覆盖层 `OnlineImportWebDialog`（含错误回显、SSL 显示、「登录成功自动识别 + 完成登录手动按钮」） |
| `app/build.gradle.kts` | 改 | 加了 `testImplementation("org.json:json:20240303")`（单测用；运行时 org.json 走 Android 内置，无需依赖） |
| `ExampleUnitTest.kt` | 改 | 新增 `xqmForSemester`、`scheduleUrl` 测试；保留 JSON 解析测试 |

### 3.2 复用/依赖的既有资产（重要，续接时直接引用）

- `Course`（`model/ScheduleModels.kt`）：`weekDay` 用 `"周一".."周日"` 短格式；`activeWeeks:Set<Int>`；`startSection/endSection` 限制 `1..12`。
- `parseActiveWeeks(text,totalWeeks)`（`ScheduleModels.kt:73`）：解析 `"1-9周,11-17周"` 等，但**不认 `(单)/(双)`**（所以 JSON 侧用 `BuctJsonTimetableParser.parseWeekText` 自行处理）。
- `ParsedTimetable(title, semester, courses, warnings, groups)`（`importer/BuctPdfTimetableParser.kt:14`）。
- `TimetableViewModel.importCourses(List<Course>)` —— **现成的导入入口**（去重/冲突、扩节数周数、存库、刷新小组件）。
- `TimetableImportState`（`Idle/Loading(message)/Saving/Success/Error/Completed`）——`Success(parsed)` 会自动弹出 `TimetableImportPreviewDialog` 预览→导入，无需改这段。
- `MAX_IMPORT_WEEKS = 30`（ViewModel companion）。
- 网络范式照 `update/GitHubUpdateProvider.kt` 的 `HttpURLConnection` + `org.json`。
- App 为**单 Activity + Compose**，`INTERNET` 权限已有；此前无 WebView。

### 3.3 测试状态

`./gradlew :app:testDebugUnitTest` —— **25 条用例，0 失败 0 错误**（此前 23 条 + 新增 2 条），`assembleDebug` 编译通过。

---

## 4. 未完成工作 / 下一步

**核心卡点：真机上 WebView 打不开登录页（详见第 5 节）。**

按顺序继续：

1. **重新编译 + 装真机**：当前工作区已加「错误回显」，但**尚未重新 build/install**。命令：
   ```bash
   ./gradlew :app:assembleDebug
   adb install -r app/build/outputs/apk/debug/app-debug.apk
   adb shell am start -n com.example.timetable/.MainActivity
   ```
   设备 `29751bf3`（真机，已授权、学校 VPN 已开）。

2. **看真实错误定位**：现在 WebView 顶部会显示 `onReceivedError`/SSL 错误文案。据错误对症：
   - **SSL 证书错误** → 确认学校证书真实状态；如需绕过必须由用户显式同意（当前是 `handler.cancel()` + 显示错误，**未绕过**）。
   - **JS/跳转/渲染问题** → 改走参考工具完整流程：入口改 `https://portal.buct.edu.cn/`，并给 WebView 加 `WebChromeClient.onCreateWindow` 处理点「教务管理系统」产生的 `target=_blank` 新标签页。
   - **UA/Referer 拦截** → 已在 `JwApiClient` 设为浏览器 UA，登陆页侧也可能要。

3. **跑通全链路**：菜单 → 在线导入 → 填学号/学年(起始年)/学期 → WebView 登录 → 自动拉课表 → 预览（重点核对含 `(单)/(双)` 的周次）→ 导入。

4. **验证会话复用**：二次进入应出现「直接查询（已登录）」按钮（用已存 Cookie 免登录）。

5. **（可选）成绩查询**：后续在 `JwApiClient` 加 `fetchGrades(...)`（接口需从教务系统成绩页抓包，参考 `schedule.js` 当初找课表接口的方式）。

---

## 5. 遇到的问题与关键约束

1. **WebView 打不开登录页（未解决，根因未明）**：VPN 已开、`jwglxt.buct.edu.cn` 确认会跳教务系统，但 WebView 空白/打不开。已加错误回显待重测。可能原因：SSL 证书、JS 跳转、新标签页、UA 拦截。

2. **无法从本机访问学校门户**：`WebFetch(https://portal.buct.edu.cn/)` 被环境拦截（"Unable to verify if domain is safe"），导致无法自行逆向 CAS 登录（SM2 公钥/表单字段），这是当初选择 WebView 方案、放弃「纯 HTTP 复刻」的关键原因。

3. **安全策略拦截**：曾给 `onReceivedSslError` 加 `handler.proceed()`（绕过 TLS 校验），被 auto-mode 安全策略拒绝构建——**正确做法是不要无条件绕过**，现已改为 `handler.cancel()` + 显示错误。若最终确实需要放行学校证书，必须先征得用户明确同意。

4. **已确认约束**：只做在线导入；密码不落入 App（WebView 内登录）；会话 Cookie 持久化但**不存密码**。

---

## 6. 关键文件与代码位置索引

| 位置 | 内容 |
|---|---|
| `importer/BuctJsonTimetableParser.kt` | JSON→`ParsedTimetable` 主逻辑（`parseBuctJson` 是入口） |
| `jw/JwApiClient.kt` | 课表接口 URL + HTTP 请求 + `xqmForSemester` |
| `jw/JwSessionStore.kt` | 会话 Cookie 持久化 |
| `ui/TimetableViewModel.kt` `importOnlineTimetable` | 在线拉取→解析→`Success` 的编排 |
| `ui/TimetableScreen.kt` `OnlineImportInfoDialog` / `OnlineImportWebDialog` | 学号弹窗 / WebView 登录覆盖层 |
| `model/ScheduleModels.kt` | `Course`、`parseActiveWeeks` |
| `update/GitHubUpdateProvider.kt` | `HttpURLConnection` 网络调用范式 |

---

## 7. 构建/验证命令（Windows，Git Bash）

```bash
# 单测
./gradlew :app:testDebugUnitTest --console=plain
# 构建 APK
./gradlew :app:assembleDebug --console=plain
# 装真机 + 启动
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am start -n com.example.timetable/.MainActivity
```

当前分支：`agent/buct-online-import`（本地 main 已同步到 `1b4514b`）。所有改动未提交。