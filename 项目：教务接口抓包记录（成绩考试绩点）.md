# 项目：教务系统接口抓包记录（成绩 / 考试 / 绩点）

> 抓包时间：2026-09-15，账号：2025050094（刘义军，数理学院 电科2504）。
> 方式：App 内浏览器真实登录 jwglxt.buct.edu.cn 后，在已登录会话中直接探测接口。
> 用途：支撑「学业」Tab 的成绩查询 / 考试查询 / 绩点查询实现，替换《学业功能实现方案》中的「阶段 0 抓包」假设值。

---

## 1. 接口总览

所有数据接口的通用约定：

- **方法**：POST（application/x-www-form-urlencoded;charset=UTF-8），URL 带 `?doType=query`。
- **必带头**：`X-Requested-With: XMLHttpRequest`（没有可能被当页面请求处理）。
- **认证**：同一 `jwglxt.buct.edu.cn` 域下带 Cookie（现有的 `JwApiClient` 直接可用）。
- **返回**：`application/json;charset=UTF-8`，结构 `{ items: [...], totalResult/totalCount }`。
- **分页参数**（jqGrid 固定格式）：`_search=false&nd={时间戳}&queryModel.showCount=50&queryModel.currentPage=1&queryModel.sortName=&queryModel.sortOrder=asc&time=0`。把 `showCount` 设大（如 100）即可一次拉全。
- **特殊状态码**：HTTP **911** 表示参数校验不过（不是证书/会话问题）。会话过期时返回的是登录页 HTML。

### 1.1 成绩查询（每门课的成绩 + 绩点 + 学分）

| 项 | 值 |
|---|---|
| 菜单 | 信息查询 → 学生成绩查询（gnmkdm=**N305005**） |
| 查询页 | `/jwglxt/cjcx/cjcx_cxDgXscj.html?gnmkdm=N305005&su={学号}` |
| 数据接口 | `POST /jwglxt/cjcx/cjcx_cxXsgrcj.html?doType=query&gnmkdm=N305005&su={学号}` |
| 请求体 | `xnm={学年起始年}&xqm={学期码}&_search=false&nd={ts}&queryModel.showCount=100&queryModel.currentPage=1&queryModel.sortName=&queryModel.sortOrder=asc&time=0` |
| 学期码 | `xnm=2025` + `xqm=3` → 2025-2026 秋冬（上学期）；`xqm=12` → 2025-2026 春夏（下学期）。**与现有 `JwApiClient.xqmForSemester()` 的 3/12/16 映射完全一致** |

响应 `items[]` 关键字段（单条完整样本见第 2 节）：

| 字段 | 含义 | 示例 |
|---|---|---|
| `kcmc` | 课程名 | 大学化学 |
| `cj` | 成绩（**数字百分制或等级制**） | `"84"` / `"B+"` / `"合格"` |
| `bfzcj` | 百分制成绩（等级制的百分制折算值） | `"84"` |
| `jd` | **该门课绩点**（接口直接给，无需自己换算） | `"3.33"` / `"0.00"` |
| `xf` | 学分 | `"2.0"`（可为 `"0"`） |
| `xfjd` | 学分绩点 = xf×jd | `"6.66"` |
| `xnm`/`xqm` | 学年 / 学期码 | `2025` / `12` |
| `kch` | 课程号 | CHM10201T |
| `kcxzmc` | 课程性质 | 公共基础必修 |
| `kclbmc` | 课程类别 | 公共基础 |
| `khfsmc` | 考核方式 | 考试 / 考查 |
| `jxbmc` | 教学班 | 大学化学-0002 |
| `xm`/`xh`/`bj`/`zymc` | 姓名/学号/班级/专业 | 刘义军 / 2025050094 / 电科2504 / 电子科学与技术 |
| `bzxx` | 备注信息（如「不统计2025」） | 不统计2025 |
| `sfjf` | 是否计绩点 | `"1"` / `"0"`（与 `jd=0.00` 配合使用） |

**重要结论**：
1. 等级制成绩 `cj` 是字符串（`B+`/`A-`/`合格`/`不及格`），百分制成绩是数字字符串（`"84"`）；解析器必须两种都兼容。
2. `bfzcj` 字段在两类成绩里都存在，可当作统一数值展示。
3. 绩点由接口直接提供（`jd` 到两位小数），**不需要也不应该本地用学校换算表计算**——官方口径与本地计算会有出入（见 1.4）。

### 1.2 考试信息查询

| 项 | 值 |
|---|---|
| 菜单 | 信息查询 → 考试信息查询（gnmkdm=**N358105**） |
| 查询页 | `/jwglxt/kwgl/kscx_cxXsksxxIndex.html?gnmkdm=N358105&su={学号}` |
| 数据接口 | `POST /jwglxt/kwgl/kscx_cxXsksxxIndex.html?doType=query&gnmkdm=N358105&su={学号}`（**与查询页同 URL**，靠 `doType=query` 区分） |
| 请求体 | `xnm={年}&xqm={学期码}&ksmcdmb_id=&kch=&kc=&ksrq=&kkbm_id=&_search=false&nd={ts}&queryModel.showCount=100&queryModel.currentPage=1&queryModel.sortName=&queryModel.sortOrder=asc&time=0` |

响应 `items[]` 关键字段：

| 字段 | 含义 | 示例 |
|---|---|---|
| `kcmc` | 课程名 | 大学物理B（I） |
| `ksmc` | 考试名称 | 期末考试 / 期中考试 / 期末考试（非考试周） |
| `kssj` | 考试时间（完整字符串，**无需自行拼接**） | `2026-07-04(08:00-10:00)` |
| `cdmc` | 考场 | 二教D-411 |
| `zwh` | 座位号 | 5 |
| `jsxx` | 监考教师 | 2006500013/王维 |
| `ksfs` | 考试方式 | 笔试 |
| `kxqm`/`kxqmc` | 学期/校区 | 12 / 北区 |
| `jxbmc` | 教学班 | 大学物理B（I）-0001 |

注意：**当前学期（`xnm=2026&xqm=3`）尚未排考，返回空 items** —— 这是正常状态，UI 需要「暂无考试安排」空态。

### 1.3 学分统计（每类课程性质的要求/已通过学分）

| 项 | 值 |
|---|---|
| 接口 | `GET /jwglxt/cjcx/cjcx_cxXsxftj.html?su={学号}` |
| 头 | `X-Requested-With: XMLHttpRequest` |
| 返回 | JSON 数组（无 items 包裹） |

字段：`kcxzmc`（课程性质名）、`fxxf`（辅修学分）、`tgxf`（通过学分）、`wtgxf`（未通过学分）。示例一行：

```json
{"kcxzmc":"公共基础必修","fxxf":"66.5","tgxf":"45.5","wtgxf":"0"}
```

该接口可支撑「学分进度」类展示，超出 v2.x 三功能范围，暂列备用。

### 1.4 绩点（GPA）——官方值来源

- 「学生学业情况查询」页面（gnmkdm=**N105515**，`/jwglxt/xsxy/xsxyqk_cxXsxyqkIndex.html`）的 **HTML 源码直接内嵌官方 GPA**：

  ```html
  平均学分绩点（GPA）：<font ...> 3.53 </font>
  ```

  抓取方式是：GET 该查询页 HTML（带 Cookie），正则提取「平均学分绩点」后的数字。不需要 POST，不需要参数。
- GPA 明细弹窗接口 `xsxyqk_cxShowGpaView.html` 需要页面内的 `xh_id` 隐藏域值，实测仍返回 **HTTP 911**（zftal 框架校验不予通过），**不可用**。绩点明细不建议依赖此接口。
- **本地计算 vs 官方值的差异**：用成绩接口的 `jd×xf/总xf` 加权计算两学期约为 **3.50**，官方页面为 **3.53**。差异来自学校对部分课程（体育、军事技能、合格制素质课等计不计数）的处理口径。**App 内应以教务页面官方值为准，或本地计算时标注「仅供参考」**。

---

## 2. 单条完整响应样本

### 2.1 成绩 items[0]（2025-2026 春夏，已裁剪无信息字段）

```json
{
  "bfzcj": "84",
  "bzxx": "",
  "cj": "B+",
  "jd": "3.33",
  "kch": "CHM10201T",
  "kclbmc": "公共基础",
  "kcmc": "大学化学",
  "kcxzdm": "05",
  "kcxzmc": "公共基础选修",
  "khfsmc": "考查",
  "kxqm": "12",
  "ksxz": "正常考试",
  "sfjf": "1",
  "xf": "2.0",
  "xfjd": "6.66",
  "xh": "2025050094",
  "xm": "刘义军",
  "bj": "电科2504",
  "zymc": "电子科学与技术",
  "xnm": "2025",
  "xnmmc": "2025-2026",
  "xqm": "12",
  "xqmmc": "2",
  "jxbmc": "大学化学-0002",
  "jsxm": "韩冬梅",
  "jgmc": "数理学院"
}
```

另一类样本（合格制，不计绩点）：

```json
{
  "bfzcj": "60",
  "bzxx": "不统计2025",
  "cj": "合格",
  "jd": "0.00",
  "kcmc": "审美的历程",
  "kcxzmc": "素质教育课程选修",
  "xf": "1.5",
  "sfjf": "0",
  "xnm": "2025",
  "xqm": "3",
  "xh": "2025050094",
  "xm": "刘义军"
}
```

### 2.2 考试 items[0]（2025-2026 春夏）

```json
{
  "kcmc": "大学物理B（I）",
  "kch": "PHY11803T",
  "ksmc": "期末考试",
  "kssj": "2026-07-04(08:00-10:00)",
  "cdmc": "二教D-411",
  "cdxqmc": "北区",
  "zwh": "5",
  "jsxx": "2006500013/王维",
  "ksfs": "笔试",
  "khfs": "考试",
  "jxbmc": "大学物理B（I）-0001",
  "xf": "5.0",
  "xm": "刘义军",
  "xh": "2025050094",
  "xnm": "2025",
  "xnmc": "2025-2026",
  "xqm": "12",
  "xqmmc": "2",
  "kkxy": "数理学院",
  "zymc": "电子科学与技术"
}
```

---

## 3. 抓到的实际数据概况

- **成绩**：2025-2026 秋冬 16 门、春夏 16 门（GPA 加权约 3.50，官方 3.53）；2026-2027 秋冬（当前学期）为空。
- **考试**：2025-2026 春夏 6 场（4 场期末 + 1 场期中 + 1 场非考试周），含时间/教室/座位/监考；当前学期为空。
- 少量课程 `xf=0`（如大学计算机、就业指导），绩点 `0.00`，成绩「合格」——属正常数据形态。

---

## 4. 对实现方案的影响（修订《学业功能实现方案》）

1. **阶段 0 完成**：接口已确认，不再需要用户另行抓包；阶段 1 开发可直接开工。
2. `JwApiClient` 需要新增 **POST form 能力**（现有仅 GET）+ 统一加 `X-Requested-With: XMLHttpRequest`。
3. 两个数据接口（成绩/考试）的学期码参数与课表完全一致，直接复用 `xqmForSemester()`。
4. **绩点不用本地换算**：成绩接口自带 `jd`；官方总 GPA 从学业情况页 HTML 提取（正则「平均学分绩点」→ 数字），本地加权值仅供后备展示并标注「仅供参考」。
5. 解析器需兼容：成绩字段是数字或等级制字符串；`xf` 为字符串；`jd` 可能缺失；`items` 可能为空。
6. HTTP 911 不是网络错误，是参数校验失败；会话过期表现为返回登录页 HTML（déjà `JwApiClient` 需识别非 JSON 响应）。